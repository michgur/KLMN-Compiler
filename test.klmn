# fixme: currently, you have to write return;, even if void

private final float poo = 2.0;
public float getPoo(float a) { return a + poo; }
private float poo2 = poo + 2.0;

public float max(float a, float b) {
    if (a > b) return a;
    return b;
}

# until I implement type checking properly, only use floats (vars & literals)
public void main(shit args) {
    float i;
    for (i = 0.0; i < 5.0; i++)
        print i;
    print max(i, getPoo(4.0));
    return;
}
